{
  "name": "untrained-momentum-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build:site": "node scripts/build.mjs",
    "check:js": "node scripts/check-js.mjs",
    "build": "npm run check:js && npm run build:site && node scripts/normalize-experience.mjs _site && node scripts/copy-platform.mjs && node scripts/qa.mjs _site",
    "check": "npm run check:js && node scripts/build.mjs --check"
  }
}
