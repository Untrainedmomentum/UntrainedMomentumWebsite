# Supabase client platform

Production database migrations are applied through the connected Supabase project (`tamyxenqhgstjorvsiym`).

The files in this directory are documentation only. Do not commit service-role or secret keys. Browser code uses the project's publishable key and relies on Supabase Auth, Row Level Security, and column grants for authorization.
